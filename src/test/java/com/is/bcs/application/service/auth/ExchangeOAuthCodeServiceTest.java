package com.is.bcs.application.service.auth;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

/**
 * 인가 코드 교환 검증 — 아직 비어 있다.
 *
 * <p>원본 테스트가 exchange 시그니처 변경을 따라오지 못해 컴파일이 막혔고, 그때 본문을 비운 채로 남았다.
 * 빈 클래스는 실행 목록에 아무것도 남기지 않아 구멍이 보이지 않으므로, 건너뛴 것으로 드러나게 표시해 둔다.
 * 인증 축 담당자가 채운다.
 */
class ExchangeOAuthCodeServiceTest {

    @Test
    @Disabled("인증 축 담당자 몫 — exchange 시그니처 변경 이후 원본 테스트가 복구되지 않았다")
    void exchange_notCoveredYet() {
    }
}
